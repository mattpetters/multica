name: Deploy (CD)

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      commit_sha:
        description: 'Full commit SHA to deploy (default: latest main)'
        required: false
        type: string

# Only one deploy at a time; cancel stale runs.
concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  BACKEND_IMAGE: ghcr.io/${{ github.repository_owner }}/multica-backend
  WEB_IMAGE: ghcr.io/${{ github.repository_owner }}/multica-web
  DEPLOY_DIR: /docker/multica

jobs:
  deploy:
    runs-on: [self-hosted, linux, x64]
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Compute metadata
        id: meta
        shell: bash
        run: |
          TARGET_SHA="${{ github.event.inputs.commit_sha || github.sha }}"
          SHA_SHORT="${TARGET_SHA:0:7}"
          IMAGE_TAG="sha-${TARGET_SHA}"
          echo "target_sha=$TARGET_SHA" >> "$GITHUB_OUTPUT"
          echo "sha_short=$SHA_SHORT" >> "$GITHUB_OUTPUT"
          echo "image_tag=$IMAGE_TAG" >> "$GITHUB_OUTPUT"
          echo "commit_msg=$(git log -1 --pretty=%s "$TARGET_SHA" 2>/dev/null || echo 'manual-deploy')" >> "$GITHUB_OUTPUT"
          echo "committed_at=$(git log -1 --format=%cI "$TARGET_SHA" 2>/dev/null || echo 'now')" >> "$GITHUB_OUTPUT"

      # ── Build images locally on VPS ──────────────────────────────
      - name: Build backend
        run: |
          docker build \
            --file Dockerfile \
            --build-arg VERSION=${{ steps.meta.outputs.sha_short }} \
            --build-arg COMMIT=${{ github.sha }} \
            --tag ${{ env.BACKEND_IMAGE }}:${{ steps.meta.outputs.image_tag }} \
            .

      - name: Build frontend
        run: |
          docker build \
            --file Dockerfile.web \
            --build-arg REMOTE_API_URL=http://backend:8080 \
            --build-arg NEXT_PUBLIC_WS_URL=wss://recurse.pro/ws \
            --build-arg NEXT_PUBLIC_APP_VERSION=${{ steps.meta.outputs.sha_short }} \
            --tag ${{ env.WEB_IMAGE }}:${{ steps.meta.outputs.image_tag }} \
            .

      # ── Deploy ──────────────────────────────────────────────────
      - name: Deploy
        id: deploy
        shell: bash
        run: |
          DEPLOY_DIR=${{ env.DEPLOY_DIR }} \
          BACKEND_IMAGE=${{ env.BACKEND_IMAGE }} \
          WEB_IMAGE=${{ env.WEB_IMAGE }} \
          IMAGE_TAG=${{ steps.meta.outputs.image_tag }} \
          COMMIT_SHA=${{ steps.meta.outputs.target_sha }} \
          COMMIT_MSG='${{ steps.meta.outputs.commit_msg }}' \
            bash ${{ env.DEPLOY_DIR }}/scripts/cd-deploy.sh

      # ── Health Check ────────────────────────────────────────────
      - name: Health check
        id: health
        shell: bash
        run: |
          sleep 10
          bash ${{ env.DEPLOY_DIR }}/scripts/cd-health-check.sh

      # ── Rollback on Failure ─────────────────────────────────────
      - name: Rollback on health check failure
        if: failure() && steps.deploy.outcome == 'success'
        shell: bash
        run: |
          echo "=== ❌ Health check failed — rolling back ==="

          PREV_TAG=$(cat /tmp/.cd-prev-tag 2>/dev/null || echo "")

          if [ -z "$PREV_TAG" ]; then
            echo "No previous tag found — cannot rollback"
            exit 1
          fi

          echo "Rolling back to: $PREV_TAG"
          DEPLOY_DIR=${{ env.DEPLOY_DIR }} \
          BACKEND_IMAGE=${{ env.BACKEND_IMAGE }} \
          WEB_IMAGE=${{ env.WEB_IMAGE }} \
          IMAGE_TAG="$PREV_TAG" \
          COMMIT_SHA="rollback" \
          COMMIT_MSG="rollback to $PREV_TAG" \
            bash ${{ env.DEPLOY_DIR }}/scripts/cd-deploy.sh

          # Health check after rollback
          sleep 10
          bash ${{ env.DEPLOY_DIR }}/scripts/cd-health-check.sh && \
            echo "⏪ Rollback successful" || \
            echo "⏪ Rollback deployed but health check still failing"

      # ── Cleanup old images ──────────────────────────────────────
      - name: Prune old images
        if: always()
        run: |
          # Remove dangling images and old build cache
          docker image prune -f --filter "until=168h" 2>/dev/null || true

      # ── Notifications ───────────────────────────────────────────
      - name: Notify deploy failure
        if: failure()
        shell: bash
        env:
          BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          if [ -z "$BOT_TOKEN" ] || [ -z "$CHAT_ID" ]; then
            echo "Telegram secrets not set — skipping notification"
            exit 0
          fi
          SHA="${{ steps.meta.outputs.sha_short }}"
          MSG='${{ steps.meta.outputs.commit_msg }}'
          URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          TEXT="🚨 *Deploy FAILED* — \`${{ github.repository }}\`%0ACommit: \`${SHA}\` — ${MSG}%0AJob: [#${{ github.run_number }}](${URL})"
          curl -s -X POST "https://api.telegram.org/bot${BOT_TOKEN}/sendMessage" \
            -d "chat_id=${CHAT_ID}" \
            -d "parse_mode=Markdown" \
            -d "text=${TEXT}" \
            -d "disable_web_page_preview=true" > /dev/null

      - name: Notify deploy success
        if: success()
        shell: bash
        env:
          BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          if [ -z "$BOT_TOKEN" ] || [ -z "$CHAT_ID" ]; then
            echo "Telegram secrets not set — skipping notification"
            exit 0
          fi
          SHA="${{ steps.meta.outputs.sha_short }}"
          MSG='${{ steps.meta.outputs.commit_msg }}'
          URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          TEXT="✅ *Deploy succeeded* — \`${{ github.repository }}\`%0ACommit: \`${SHA}\` — ${MSG}%0AJob: [#${{ github.run_number }}](${URL})"
          curl -s -X POST "https://api.telegram.org/bot${BOT_TOKEN}/sendMessage" \
            -d "chat_id=${CHAT_ID}" \
            -d "parse_mode=Markdown" \
            -d "text=${TEXT}" \
            -d "disable_web_page_preview=true" > /dev/null
